# Nucleo del framework + plugins oficiales usados (Apache-2.0)
livekit-agents[silero,openai]~=1.6
python-dotenv~=1.0

# STT local (faster-whisper, MIT)
faster-whisper~=1.1

# Cliente HTTP para el TTS Piper y el LLM via Ollama (OpenAI-compatible)
httpx~=0.27
